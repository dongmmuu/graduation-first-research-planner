# 证据与组内文献审计

## Evidence Record

每个影响方向成立、资源可用或毕业认定的事实使用稳定记录：

```text
Evidence ID: ER-YYYYMMDD-shortslug-NN
作用域: L 文献事实 | R 当前资源 | G 毕业规则 | U 用户偏好 | X 实验/数据结果
主张:
等级: A | B | C | D | E
来源类型: 正式文件 | 完整论文 | 原始记录 | 负责人确认 | 数据/代码 | 摘要/网页 | 转述 | 推断
来源与定位: 文件名、页码/章节/图表/记录日期；无法定位则写“来源未定位”
支持什么:
不支持什么:
矛盾/限制:
有效期或复核日:
下一步:
```

`ER` ID 在后续路线卡、评分和决策中复用。不同来源或不同现实条件不能塞进同一记录。

## 证据升级规则

- A 只用于当前、直接、可核对的证据。口头转述通常不能单独升 A。
- B 需要至少两条相互独立的间接证据；同一论文的正文与补充材料不算两条独立证据。
- C 是关键但未确认，不是“差不多可用”。
- D 必须同时记录“如果错了会怎样”和最小验证动作。
- E 不得参与确定性结论或被自动赋分。
- 资源记录过了有效期或关键条件变化后标 `已过期`，重新核实，不能沿用旧等级。

## 文献身份与主张支持是两道检查

1. **身份核验**：标题、作者、年份、期刊/会议、DOI/arXiv/数据库记录是否匹配。
2. **内容核验**：原文方法、结果或限制是否真的支持当前句子；记录定位和允许的表述范围。

身份核验通过只说明文献存在。若只有摘要、搜索片段或网页占位符，只能用于发现和待阅读清单；除非主张本身仅涉及这些材料明确展示的元数据，不用于支撑方法可行性或结果结论。

## 上传组内文献的固定流程

### 1. 建立材料清单

记录文件名、年份、文献类型、是否全文、是否可读取、是否含补充材料。扫描失败、缺页、图片模糊或表格无法解析必须明确说明。

### 2. 每篇提取

- 研究对象、核心问题和最小研究单元；
- 样品/受试者/数据来源与规模；
- 关键方法、设备、软件、参数、质量控制和统计方法；
- 外协、特殊耗材、审批、数据许可和关键人员依赖；
- 主要结果、失败/限制、作者明确提出的适用边界；
- 可核对的页码、章节、图表或补充材料定位。

### 3. 分开输出“过去能力”和“当前条件”

| 能力/条件 | 论文证明的过去事实 | 当前状态 | 等级 | 现实补证动作 |
|---|---|---|---|---|
| 某表征 | 方法部分显示曾使用 | 当前设备与排期未知 | C | 问平台负责人并取得日期 |

### 4. 可复用分级

- `直接复用`：当前资源、人员指导、权限和时间均已确认。
- `部分复用`：方法有基础，但至少一个关键条件待补证。
- `仅作背景`：只能支持研究动机或历史能力。
- `不建议复用`：关键条件已失效、不可得、不合规或不适合剩余时间。

### 5. 论文到路线的转换

每条候选路线必须引用 Evidence ID，并注明：复用了什么、新增了什么、哪项当前条件仍未被论文证明。不得从“组里发过”直接跳到“用户能按时完成”。

## 矛盾处理

- 用户口述与文件冲突：标 `需复核`，优先寻求学校文件或责任人确认。
- 多篇论文结果冲突：保留冲突，不平均成一个结论；说明实验对象、方法和时间差异。
- 当前资源与历史文献冲突：当前可核对证据优先，但保留历史记录用于解释变化。
- 找不到支持：降级表述、转为假设或删除，不补造引用。

## 敏感材料

- 默认仅为当前任务读取，不向外部网站、检索服务或公开仓库发送。
- 输出用于展示或分享时，隐去姓名、单位、导师、基金号、设备编号、样本标识、未公开结果和可反推课题组的信息。
- 用户要求联网核验公开论文时，只发送公开标识符或公开题录；不要上传组内原文件，除非用户单独明确授权。
