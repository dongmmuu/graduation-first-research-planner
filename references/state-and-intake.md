# 状态、提问与输入合同

## 最小输入格式

用户不必完整填写。先从现有对话和上传材料中提取；缺失项标 `未知`，只问会改变决策的内容。

```text
[模式] /开题 | /审题 | /救火 | /毕业规划
[毕业] 学位/年级；预计毕业；开题/中期/成果认定/送审/答辩日期；学校要求；导师要求；已完成成果
[方向] 当前题目/研究兴趣；必须保留或不可接受的内容
[个人] 已掌握方法；可获得指导；每周可投入时间；当前困难
[资源] 人员；经费；设备/平台；样品/受试者；数据/代码；合作/外协；审批/授权
[材料] 学校文件；组内论文；开题书；实验记录；设备清单；数据说明；导师原话
[偏好] 保毕业/发表/申博/就业优先级；风险承受；评分权重
```

## 会话状态快照

维护以下对象，但中间轮只显示变化和阻塞项。不要声称这些状态会跨会话永久保存；用户要求落盘时才写文件。

```yaml
session:
  mode: 开题|审题|救火|毕业规划
  as_of: YYYY-MM-DD
  question_round: 0|1|2
graduation_constraints: []
evidence_records: []
resources: []
questions: []
routes: []
decisions: []
```

### 资源记录

| 字段 | 说明 |
|---|---|
| ID | `RES-设备-01` 等稳定 ID |
| 类别 | 人/钱/设备/样品/数据/技术/时间/审批/合作/出口 |
| 当前状态 | 已确认/部分确认/待确认/不可用/未知/已过期 |
| 证据 | Evidence ID、负责人、来源与日期 |
| 获取成本 | 钱、排队、培训、采购或沟通成本 |
| 最晚确认日 | 超过此日即触发降级或切线 |
| 替代 | 无则明确写“无” |
| 单点故障 | 是/否及原因 |

### 问题记录

| ID | 对象/视角 | 问题 | 状态 | 已有证据/日期 | 会改变什么 | 未回答分支 |
|---|---|---|---|---|---|---|
| Q1 | 平台老师/执行 | 关键设备最早何时可用？ | 开放 | — | 时间门、R1 排名 | 过切换日则转 R2 |

状态只使用：`开放`、`部分回答`、`已确认`、`被否定`、`需复核`、`已过期`。新证据更新原 ID 并保留变化，例如：`开放 → 已确认（平台邮件，日期）`。

### 决策记录

记录 `决定`、`依据 Evidence ID`、`适用假设`、`触发复核的条件` 和 `日期`。新信息推翻旧决定时，保留旧记录并写变更原因。

## 问题优先级

问题按以下顺序选择：

1. 能确认或否决毕业、合规硬门；
2. 能确认核心资源和最晚可用时间；
3. 能让候选路线互换排序；
4. 能触发 Plan B/C 或停止；
5. 只是补充背景但不改变决策的问题不进入首轮。

按需覆盖五个视角：学校/毕业认定、导师/团队、用户/执行、评委/审稿人、平台/合作/合规。不要机械地每个视角都问一题。

每轮 3—5 题，每题使用：

```text
Q1 [问谁] 问题？
为什么现在要问：它决定哪个门或哪条路线。
若暂时答不上：采用哪个条件分支，何时必须确认。
```

## 收敛与停止

- 第 0 轮：只提取材料并生成首轮问题。
- 第 1 轮：更新状态，删除已失去决策价值的问题，必要时问第二轮。
- 第 2 轮：停止追加问题，输出条件化结论和最小补证清单。
- 若新证据与旧证据冲突，将相关项标 `需复核`；在复核前不得升级为 A。
- 若检索或追问没有产生新证据，认定“信息饱和”，用现有证据收尾，不无限循环。
- 只有用户提供新证据、关键状态过期、时间节点变化或权重变化时才重新开评估轮。

## 中间轮显示格式

```text
状态变化：Q2 已确认；RES-设备-01 从待确认变为不可用。
当前阻塞：时间门；关键数据授权未知。
本轮问题：Q3—Q5。
若本轮无回复：采用 R2 条件方案，并在日期 X 前切换。
```
