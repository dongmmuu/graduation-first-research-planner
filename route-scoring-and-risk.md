# 路线、评分、红队与倒排

## 研究路线卡

```text
路线 ID / 类型: R1；保底|主线|扩展|替代
研究问题:
暂定假设或预期关系:
为什么值得做:
文献证据: Evidence IDs
当前条件证据: Evidence IDs
缺失条件:
最低认可交付:
MVP:
什么结果支持继续:
什么结果证伪/触发降级:
新增依赖与单点故障:
最晚确认/切换日:
决定: 启动|补证|降级|停止
```

候选路线应真正不同，不得只替换标题。通常包括最大化复用的保底线、只新增一个可控变量的主线、有额外创新和依赖的扩展线，以及必要时的数据/方法/比较/理论替代线。

## 五门检查

| 门 | PASS | CONDITIONAL | FAIL |
|---|---|---|---|
| 毕业 | 最低成果明确且被认可 | 认定口径待责任人确认 | 已知不被认可或赶不上认定 |
| 资源 | 核心条件已确认或替代已确认 | 可补证且早于切换日 | 核心条件不可得且无替代 |
| 证据 | 关键主张有可定位证据 | 缺口明确且可低成本补齐 | 方向建立在无依据或矛盾未解的主张上 |
| 时间 | 完整关键路径含缓冲且可完成 | 依赖日期待确认但有余量 | 即使顺利也超过硬节点 |
| 合规 | 审批/授权已具备或无需 | 可在启动前取得 | 无法取得、已违规或窗口已关闭 |

任一 FAIL 且无替代即 RED。评分不能把 RED 提升为主线。

## 评分维度与预设

每项 0—5 分，分数越高越有利；`风险/合规` 表示“越安全、越可控分越高”。必须写评分依据。

| 维度键 | 默认 | 实验重型 | 数据/计算 | 伦理/审批重型 |
|---|---:|---:|---:|---:|
| completion | 25 | 30 | 25 | 25 |
| resources | 20 | 20 | 15 | 15 |
| foundation | 15 | 15 | 10 | 10 |
| deliverable | 15 | 15 | 20 | 15 |
| safety | 10 | 10 | 10 | 20 |
| reproducibility | 5 | 5 | 10 | 5 |
| clarity | 5 | 3 | 5 | 5 |
| novelty | 5 | 2 | 5 | 5 |

优先采用用户权重。自定义权重不等于 100 时先归一化并说明；不要擅自改变维度方向。

### 未知项与覆盖率

未知项填 `null`，不填 3。按已知维度计算暂定分：

```text
暂定分 = 20 × Σ(权重 × 0—5分) / Σ(已知项权重)
覆盖率 = Σ(已知项权重) / Σ(全部权重) × 100%
```

输出格式：`暂定分 78/100；评分覆盖率 65%；AMBER`。覆盖率低于 70%，或任何核心门仍未知时，只做暂定排序，不宣布唯一最佳路线。

使用 `scripts/score_routes.py` 可计算自定义权重、覆盖率和可排名状态。输入示例：

```json
{
  "preset": "experimental",
  "routes": [
    {
      "id": "R1",
      "gate": "amber",
      "veto": false,
      "scores": {
        "completion": 4,
        "resources": 3,
        "foundation": 4,
        "deliverable": 4,
        "safety": null,
        "reproducibility": 3,
        "clarity": 4,
        "novelty": 2
      }
    }
  ]
}
```

### 敏感性

若前两条路线相差少于 5 分、任一覆盖率不足 80%，或用户在学科预设间犹豫，至少再用一个合理预设复算。排序改变时，不给单一结论；指出哪个权重或未知条件造成翻转。

## 红队审题

按相关性选择攻击面，并写“风险—证据—最早信号—动作—切换日”：

- 学校/毕业：成果认定、作者/期刊/专利限制、截止日是否误读；
- 导师/团队：要求反复变化、关键指导依赖、离组人员、成果归属；
- 用户/执行：方法不会、训练周期、每周时间、健康与负荷；
- 资源：设备排期、特殊样品、一次性数据、采购、经费、算力；
- 评委/审稿人：问题过散、样本量/对照不足、结论超出证据；
- 合规：伦理、安全、隐私、数据许可、合作授权；
- 结果：不显著、不可复现、质量控制失败、核心假设被证伪。

否决红线包括：最低交付确定不被认可；即使一切顺利也超过硬节点；核心条件确定不可得且无替代；必要审批或授权无法取得。待确认不是自动否决，但必须有负责人、最晚确认日和失败分支。

## MVP、Plan B/C

MVP 不是“少做一点”，而是能产生决策信号并保留毕业价值的最小研究单元：

- 最小问题与边界；
- 最少样品/数据/实验/证明步骤；
- 对照、质量控制和分析方法；
- 首轮成功/失败判据；
- 可形成的最低认可交付；
- 继续、降级、停止条件。

`Plan B` 应去掉至少一个核心依赖但尽量保留研究问题；`Plan C` 可缩小问题或切换数据/方法，以优先保住认可交付。每个 Plan 都要有触发信号、切换日和切换后第一步。

## 成果反推与时间倒排

固定从后向前：

```text
答辩/毕业
← 送审与行政材料
← 导师定稿与返修
← 可提交成果/认定
← 图表、统计、数据与复现包
← MVP 后的必要扩展
← 资源确认、培训、采购、审批
```

同时给三个时间尺度：未来 72 小时的确认动作、未来 2—4 周的 MVP、到硬节点的月度里程碑。缓冲不得只写“预留时间”，要说明用于排队、失败重做、导师反馈、投稿/认定或行政流程。
